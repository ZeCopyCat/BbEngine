#include "Buffers.h"

namespace Bb {

	/*
	std::array<Buffer, 6> BufferManager::s_buffers = {

		Buffer(), // Visible Buffer
		Buffer(), // World Buffer
		Buffer(), // UI Buffer
		Buffer(), // Custom Buffers
		Buffer(),
		Buffer()

	};
	*/

}